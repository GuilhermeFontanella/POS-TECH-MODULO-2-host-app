import { ThemeApplierDirective } from './theme-applier.directive';

describe('ThemeApplierDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeApplierDirective();
    expect(directive).toBeTruthy();
  });
});
